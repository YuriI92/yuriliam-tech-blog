<section class="px-4 py-2">
    {{> post-details post}}
</section>
<section class="px-4">
    <div class="border border-dark">
        <h3 class="bg-dark px-3 py-2 text-info">Comments</h3>
        {{#each post.Comments as |comments|}}
        <p class="px-3 py-2 text-dark">{{comment}}<br/>—{{user.username}}, {{format_date created_at}}</p>
        {{/each}}
    </div>
    {{#if loggedIn}}
    <form class="comment-form">
        <label for="comment-text">Comment</label>
        <input type="text" id="comment-text" name="comment-text" data-user="{{session_userId}}" />
        <button type="submit" id="add-comment">Submit</button>
    </form>
    {{/if}}
</section>

<script src="/javascript/add-comment.js"></script>