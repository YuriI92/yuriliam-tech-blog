<article>
    <div>
        <a href="/post/{{id}}">
            <h2>{{title}}</h2>
        </a>
        <p>Posted by {{user.username}} on {{created_at}}</p>
    </div>
    <p>{{contents}}</p>
</article>