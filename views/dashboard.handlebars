<section>
    {{#each posts as |post|}}
    <div>
        <a href="/post/{{id}}">{{title}}</a>  
        <p>{{created_at}}</p>
    </div>
    {{/each}}
</section>
<button type="button" id="add-post">+ New Post</button>