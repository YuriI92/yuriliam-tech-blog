<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css" integrity="sha384-TX8t27EcRE3e/ihU7zmQxVncDAy5uIKz4rEkgIXeMed4M0jlfIDPvg6uqKI2xXr2" crossorigin="anonymous">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <title>Yurilium Tech Blog</title>
</head>
<body>
    
    <header class="p-2 bg-dark">
        <nav class="p-2">
            <a href="/" class="p-1 mx-3 text-info">Home</a>
            {{#if loggedIn}}
            <a href="/dashboard" class="p-1 mx-3 text-info">Dashboard</a>
            <button id="logout-btn text-info">Logout</button>
            {{else}}
            <a href="/login" class="p-1 mx-3 text-info">Dashboard</a>
            <a href="/login" class="p-1 mx-3 text-info">Login</a>
            {{/if}}
        </nav>
        <div class="container row text-center">
            {{#if dashboard}}
            <h1 class="bg-info px-1 py-1 ml-4 col-4">Your Dashboard</h1>
            {{else}}
            <h1 class="bg-info px-1 py-1 ml-4 col-4 rounded shadow">Yurilium Tech Blog</h1>
            {{/if}}
        </div>
    </header>

    <main>
        {{{ body }}}
    </main>

    <footer class="bg-dark text-info text-center p-2 fixed-bottom">
        <h2>Thank you for your visit⚜️</h2>
    </footer>

    {{#if loggedIn}}
    <script src="/javascript/logout.js"></script>
    {{/if}}
</body>
</html>
